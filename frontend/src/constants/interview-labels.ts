/** Arabic labels for interview session status (backend values). */
export const INTERVIEW_STATUS_LABEL: Record<string, string> = {
  active: "جارية",
  completed: "مكتملة",
  reviewed: "تمت المراجعة",
}

/** Arabic labels for HR decision values (backend values). */
export const HR_DECISION_LABEL: Record<string, string> = {
  shortlist: "مقبول مبدئياً",
  reject: "رفض",
  "needs review": "يحتاج مراجعة",
}
